<?xml version="1.0" encoding="UTF-16"?>
<!--
  Task Scheduler definition for the SBV2 watchdog.
  Import with (run an elevated PowerShell/cmd):
    schtasks /create /tn "SBV2Watchdog" /xml "D:\Pico\sbv2_watchdog\SBV2Watchdog.task.xml" /ru "<USER>" /rp "<PASSWORD>"
  Edit BEFORE import:
    - <UserId> below: set to the account that owns the SBV2 / GPU session.
    - <Command>/<Arguments> path to sbv2_watchdog.ps1 on the main PC.
  See README.md for the Register-ScheduledTask alternative (no XML editing).
-->
<Task version="1.4" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <Description>Polls SBV2 /voice and auto-restarts the TTS server if it hangs (Pico Phase X).</Description>
    <URI>\SBV2Watchdog</URI>
  </RegistrationInfo>
  <Triggers>
    <LogonTrigger>
      <Enabled>true</Enabled>
    </LogonTrigger>
  </Triggers>
  <Principals>
    <Principal id="Author">
      <!-- Set to the user that runs SBV2 (has the GPU/desktop session). -->
      <UserId>REPLACE_WITH_USERNAME</UserId>
      <LogonType>InteractiveToken</LogonType>
      <RunLevel>HighestAvailable</RunLevel>
    </Principal>
  </Principals>
  <Settings>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>false</StopIfGoingOnBatteries>
    <AllowHardTerminate>true</AllowHardTerminate>
    <StartWhenAvailable>true</StartWhenAvailable>
    <RunOnlyIfNetworkAvailable>false</RunOnlyIfNetworkAvailable>
    <AllowStartOnDemand>true</AllowStartOnDemand>
    <Enabled>true</Enabled>
    <Hidden>false</Hidden>
    <RunOnlyIfIdle>false</RunOnlyIfIdle>
    <WakeToRun>false</WakeToRun>
    <!-- Long-running loop: no execution time limit. -->
    <ExecutionTimeLimit>PT0S</ExecutionTimeLimit>
    <Priority>7</Priority>
    <RestartOnFailure>
      <!-- 失敗時 5 分後にリトライ、最大 3 回。 -->
      <Interval>PT5M</Interval>
      <Count>3</Count>
    </RestartOnFailure>
  </Settings>
  <Actions Context="Author">
    <Exec>
      <Command>powershell.exe</Command>
      <!-- Edit the -File path to where sbv2_watchdog.ps1 lives on the main PC. -->
      <Arguments>-NoProfile -ExecutionPolicy Bypass -File "D:\Pico\sbv2_watchdog\sbv2_watchdog.ps1"</Arguments>
    </Exec>
  </Actions>
</Task>
