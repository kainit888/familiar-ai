# 設計書 v4.3 素案 セルフレビュー

**作成**: 2026-05-20 (外出期間 2 / Day 2 / タスク 10)
**レビュー対象**: `/home/pico/pico_v3_docs/設計書_統合版_v4.3_素案.md` (1932 行)
**比較対象**: `/home/pico/pico_v3_docs/設計書_統合版_v4_pico_v3.md` (v4.2、1462 行)
**位置づけ**: カイニット帰宅後のレビューに先立つ自己点検。**修正提案を列挙するのみで、本文への書き込みは実行しない**。
**レビュアー**: Claude Code (素案作成者と同一だが、独立した視点で読み直す)

---

## 0. 全体サマリ

| 項目 | 値 |
|---|---|
| v4.2 行数 | 1462 |
| v4.3 素案 行数 | 1932 |
| 差分 | **+470 行** (約 +32%) |
| 新規追加章 | 第 24 章 (二層分離)、第 25 章 (upstream 同期)、第 15-1〜15-3 (ディレクトリ再構成) |
| 大幅書き換え章 | 第 14-4 / 14-5 / 14-7 章 (Phase C-3 / C-4 確定値反映) |
| 軽微追記 | 第 14-1 / 14-2 / 第 12 章 v4.3 補足 / 第 19 章 Phase C 脚注 / 第 22 章 参考リンク |
| 章番号衝突 | **なし** (既存 1〜23 章を維持、追加は 24・25 のみ) |
| 重大欠陥 | **なし** |
| 微修正候補 | **6 件** (下記 5 章) |

総評: **設計書として確定版への昇格に値する内容**。Phase C-2〜C-4 の実装事実を漏れなく拾い、二層分離・upstream 同期という長期運用方針を明文化できている。ただし「v4.3 注釈の表記揺れ」「第 16-1 章のディレクトリ表記乖離」など、確定版前に整える価値のある微修正がいくつか残る。

---

## 1. 章構成の整合性

### 1-1. 章番号

v4.2 と v4.3 の章番号を完全マッピング：

| 章 | v4.2 | v4.3 | 状態 |
|---|---|---|---|
| 0 | 設計哲学 | 設計哲学 | 変更なし |
| 1-2 | システム概要 / HW | システム概要 / HW | 14-7 章で go2rtc 場所追記、ほぼ維持 |
| 3 | アーキテクチャ | アーキテクチャ | 「改名」記述に注記「(第 24 章参照、二層併存に訂正)」追加 |
| 4-6 | モデル / 採用範囲 / embodied-claude | 同左 | 変更なし |
| 7 | ピコ独自の追加要件 | 同左 | 7-2 / 7-3 に第 14-5 / 14-4 章への注記追加 |
| 8-11 | メモリ / 内受容感覚 / sociality / 自律実行 | 同左 | 変更なし |
| 12 | 音声パイプライン | 同左 | **v4.3 補足ブロック** 追加 (Tapo RTSP 経路の補足) |
| 13 | Discord 統合 | 同左 | 変更なし (Phase D 着手前、現状維持で OK) |
| 14 | Tapo C210 統合 | 同左 (大幅書き換え) | 14-4 / 14-5 を 9 / 11 サブ節に細分化 |
| 15 | ディレクトリ構造 | 同左 (大幅書き換え) | 15-1 / 15-2 / 15-3 サブ節を新設 (二層併存ツリーに) |
| 16-18 | OS 権限 / ペルソナ / コーディング規則 | 同左 | 16-1 章テーブルに `familiar_agent/` 行追加 |
| 19 | 実装フェーズ | 同左 | Phase C 直下に v4.3 脚注 (C-0〜C-4 進捗) 追加 |
| 20-23 | Claude Code 指示 / 残課題 / 参考リンク / 関門制 | 同左 | 22 章に go2rtc リンク追加 |
| **24** | (なし) | **人格レイヤー二層分離原則** | **v4.3 新規** |
| **25** | (なし) | **upstream 同期戦略** | **v4.3 新規** |

→ **章番号体系は完全に整合**。既存番号衝突なし、跳びなし。

### 1-2. サブ節番号 (14-4-N / 14-5-N)

v4.3 で新設したサブ節:

- 14-4-1 〜 14-4-9 (連番、欠番なし)
- 14-5-1 〜 14-5-11 (連番、欠番なし)

→ **サブ節番号も整合**。

---

## 2. 新規追加章の評価

### 2-1. 第 24 章 (人格レイヤー二層分離原則) — 約 128 行

**目的**: Phase C-2 commit `e015832` 以来の実態 (二層併存) を明文化し、v4.2 第 3 章 line 195 の「改名」記述を訂正する。

**評価**: ◎ (確定版に昇格して良い品質)
- 6 サブ節 (24-1 背景 / 24-2 定義 / 24-3 配置 / 24-3-1 禁則 / 24-4 侵入点 / 24-5 テスト / 24-6 訂正) で網羅
- 図 (24-2): 二層と一方向 import の関係が一目で分かる
- 表 (24-3, 24-4): 各層に置くもの / 侵入 7 ファイル一覧
- 上流 PR 還元候補度 (24-4) を明示 — 25 章 25-5 と整合

**気になる点**: なし。本章は確定版に持ち上げ可能。

### 2-2. 第 25 章 (upstream 同期戦略) — 約 90 行

**目的**: 設計書本体に upstream 同期の手順 / コンフリクト解決方針 / PR 還元方針を明文化する (これまで実装計画書側にしか書かれていなかった)。

**評価**: ◎ (確定版に昇格して良い品質)
- 5 サブ節 (25-1 トポロジー / 25-2 手順 / 25-3 コンフリクト / 25-4 タイミング / 25-5 PR 還元) で網羅
- 図 (25-1): upstream → pico fork → ローカルの 3 層が一目
- 表 (25-3): ファイル別の発生確率と解決方針 — `src/pico_agent/` の構造的回避が明示されている
- PR 還元候補 (25-5) に commit hash (`f8bface`, `c8fdccb`, `1733c7e`) が紐づいている

**気になる点**: なし。**外出期間中に 0 件と判明した「upstream/develop 先行」の事実は本章には書かれていない**が、これは「同期戦略 = 将来発生したときの手順」なので書く必要なし。`docs/upstream_diff_2026-05-20.md` の方で補完されている。

### 2-3. 第 15-1 / 15-2 / 15-3 (ディレクトリ構造、二層併存に書き換え)

**目的**: v4.2 のディレクトリツリー (改名前提) を、二層併存の実態に書き換える。

**評価**: ○ (確定版昇格可、ただし 1 点要確認)
- 15-2 のツリーは二層併存を実態通りに記述している
- 15-3 で 24-6 章へのポインタを明示している → 重複回避の意図が見える

**気になる点**:
- **15-2 ツリーで `src/pico_agent/` の直下に `core/` がないことを明示** している点は二層分離原則と整合 (= `core/` は `src/familiar_agent/` 配下のみ)
- ただし **第 16-1 章テーブルに「`pico_v3/src/pico_agent/core/tools/`」という行が残っている** (1352 行目)
  - これは v4.2 から流用した行で、二層分離原則では「`src/pico_agent/core/tools/`」というディレクトリは**存在しない** (core は `familiar_agent/` 配下のみ)
  - → **要修正**: 16-1 章テーブルの該当行を「`pico_v3/src/familiar_agent/tools/`」に置き換えるか、削除する

---

## 3. 大幅書き換え章の評価

### 3-1. 第 14-4 章 (STT、9 サブ節に細分化) — 約 100 行

**目的**: Phase C-4 で確定した「ffmpeg silencedetect VAD」「RTSP URL 解決順」「環境変数 7 キー」を反映する。

**評価**: ◎
- 14-4-1 (経路図) → 14-4-9 (利点) まで論理順
- 14-4-2 (VAD バックエンド決定表) で「採用 / 不採用 / 保留」が明示
- 14-4-3 (ffmpeg コマンド) は実装と完全一致 (Phase C-4 commit `43419a0` の `_build_ffmpeg_rtsp_cmd` と整合)
- 14-4-5 (URL 解決順 + マスク) は実装と整合
- 14-4-6 (環境変数 7 キー) は `.env.example` (Phase C-4) と整合

**気になる点**: なし。

### 3-2. 第 14-5 章 (TTS、11 サブ節に細分化) — 約 150 行

**目的**: Phase C-3 で確定した「POST API」「30 字分割」「暖機」「ffmpeg 前処理」「model_name クエリ」「go2rtc 常駐先」を反映する。

**評価**: ◎
- 14-5-1 (概要) → 14-5-11 (フォールバックチェーン) まで論理順
- 14-5-3 (go2rtc バージョン v1.9.14、常駐先確定) と 14-7 (全体図) が整合
- 14-5-5 (POST API、body 空、URL エンコード) は Phase C-3 実装と一致
- 14-5-7 (30 字 / `。→！→？→、→\n` 優先順位) は実装と一致
- 14-5-9 (`model_name` クエリ確定) は重要な訂正

**気になる点**:
- **14-5-8 (暖機)** で「Phase D で常駐ディレクトリ移行を検討」と書かれている → 21 章 (残課題) に項目化されていない
- → **追加候補**: 21-8 として「TTS 暖機用 WAV の常駐ディレクトリ (`/tmp/` → `~/.pico_v3/`)」を残課題に追記すると整合性が上がる (任意、必須ではない)

### 3-3. 第 14-7 章 (音声 I/O 全体図、go2rtc 常駐先確定)

**評価**: ○
- 図と本文 (v4.3 確定事項) が整合
- 「go2rtc は **メイン PC `D:\Pico\go2rtc\` で常駐**」と明示

**気になる点**: なし。

---

## 4. 参照リンクの正しさ

各章間の相互参照を確認：

| 参照元 | 参照先 | 結果 |
|---|---|---|
| 第 3 章 line 216 注記 | 第 24 章 | ✓ 存在 |
| 第 7-2 章 注記 | 第 14-5 章 | ✓ 存在 |
| 第 7-3 章 注記 | 第 14-4 章 | ✓ 存在 |
| 第 12 章 v4.3 補足 | 第 14-4 章 | ✓ 存在 |
| 第 15-1 章 | 第 24 章 | ✓ 存在 |
| 第 15-3 章 | 第 24-6 章 | ✓ 存在 |
| 第 14-5-4 章 | 第 14-2 章 5 番目 | ✓ 存在 |
| 第 24-6 章 | 第 15-2 章 / 第 16-1 章 | ✓ 存在 (16-1 は 3-3 の指摘あり) |
| 第 25-2 章 | 実装計画書 2-4 章 | ✓ 外部参照、実在 |
| 第 25-5 章 | commit `f8bface`, `c8fdccb`, `1733c7e` | ✓ git log で実在確認可 |

→ **章内参照リンクはすべて整合**。Commit hash も実在。

---

## 5. 修正候補 (帰宅後カイニット判断、本セッションでは実行しない)

優先度順:

### 5-1. 【優先度 高】第 16-1 章テーブルの「`pico_v3/src/pico_agent/core/tools/`」行

**問題**: 二層分離原則 (24-2) では `core/` は `src/familiar_agent/` 配下のみ。`src/pico_agent/core/tools/` というパスは実在しないか、実在してはならない。

**修正案**:
```
| `pico_v3/src/pico_agent/core/tools/` | ✅ R/W | 読み取りのみ | コア tool は手動編集 |
                  ↓
| `pico_v3/src/familiar_agent/tools/` | ✅ R/W | 読み取りのみ | (v4.3) コア tool は familiar_agent 配下、手動編集 |
```

または該当行を削除し、24-2 章の二層図 + 24-4 章の侵入点表で十分とする。

### 5-2. 【優先度 中】v4.3 注釈の表記揺れ

現状の注釈バリエーション (4 種類混在):
- `(v4.3 確定)` — 14-4-2 / 14-4-3 / 14-4-5 / 14-4-6 / 14-5-3 / 14-5-5 / 14-5-6 / 14-5-7 / 14-5-9 / 14-5-10
- `(v4.3 新規)` — 14-5-8
- `(v4.3 追記)` — 14-1 章
- `※v4.3 確定差し替え` — 14-4 / 14-5 章のタイトル
- `(v4.3 で xxx)` — 第 14 章タイトル「(v4.3 で 14-4 / 14-5 / 14-7 を確定差し替え)」、第 14-7 章「(v4.3 で go2rtc 常駐先を確定)」

**修正案** (確定版昇格時): 2 種類に統一する
- 章/節タイトル末: `(v4.3 確定)` または `(v4.3 新規)`
- 本文中追記: `(v4.3 追記)`

「※v4.3 確定差し替え」は冗長 (タイトルが既に章名なので、別マークが要らない) → `(v4.3 確定)` に統一推奨。

### 5-3. 【優先度 中】第 15-3 章と第 24-6 章の役割分離説明

15-3 章は「重複を避けるため、第 3 章本文には注記のみ、訂正の根拠と詳細は第 24-6 章で明示」と書きつつ、15-3 章自体に短い訂正説明が書かれている。

**修正案**: 15-3 章は表題のみ残してポインタにする (`→ 第 24-6 章を参照`) か、24-6 章を逆に 15-3 章へ寄せて 24-6 をポインタにする。

軽微なので、必須ではない。

### 5-4. 【優先度 低】第 21 章 (残課題) に Phase C-3 由来項目を追加

Phase C-3 / C-4 で発生した「**TTS 暖機用 WAV の常駐ディレクトリ判断**」「**silencedetect 閾値の実機チューニング**」を残課題化すると、設計書として「Phase 進行中に判断保留した項目」のトレーサビリティが完結する。

**修正案** (21-8, 21-9 として追記):
- 21-8. TTS 暖機 WAV の常駐ディレクトリ (`/tmp/pico_v3_warmup.wav` → `~/.pico_v3/`)、Phase D 着手時に判断
- 21-9. silencedetect noise/min_silence 閾値の実機チューニング、Phase D の TUI 実機検証時に判断

軽微、必須ではない。

### 5-5. 【優先度 低】第 22 章 (参考リンク) に CHANGELOG_PICO.md の参照を追加

24-4 章および 25-5 章で `CHANGELOG_PICO.md "上流マージ受入れポリシー" / "上流 PR 候補"` を参照しているが、22 章には CHANGELOG_PICO.md の参照がない。

**修正案** (22 章末に追記):
```
- **CHANGELOG_PICO.md**: `/home/pico/pico_v3/CHANGELOG_PICO.md` (ピコ独自 fork 差分 changelog、v4.3 で参照を集約)
```

軽微、必須ではない。

### 5-6. 【優先度 低】Day 2 朝の上流 diff 結果 (0 件) の脚注

第 25 章 「upstream 同期戦略」は将来発生時の手順を書いているが、**現時点で merge 不要 (`upstream/develop` 先行 0 件、`docs/upstream_diff_2026-05-20.md`)** という観測結果は本文に含まれていない。

**修正案** (25 章末か 25-4 末に脚注):
```
> **(v4.3 観測ノート、2026-05-20)** 現時点で `upstream/develop` は `pico-base` 共通先祖から先行コミット 0 件。
> 同期は不要、次に upstream が進行した時点で 25-2 の手順を実行する。詳細は `docs/upstream_diff_2026-05-20.md`。
```

これがあると、確定版を読む人が「今すぐ merge すべきか?」を判断しやすい。

軽微、必須ではない。

---

## 6. 冗長性チェック (削減候補)

レビューの結果、**冗長と判断する章はなし**。

念のため確認した点:
- 7-2 / 7-3 章 (TTS / STT adapter 概要) と 14-4 / 14-5 章 (詳細) → 役割分離あり、冗長ではない (7 章 = ピコ独自要素の俯瞰、14 章 = Tapo C210 統合の詳細)
- 15-3 と 24-6 → 役割分離あり (5-3 参照、ただし若干重複あり、軽微)
- 24 章と 25 章 → 別概念 (二層 vs upstream 同期)、独立した章として残すべき

---

## 7. 足りない章 / 内容 (帰宅後カイニット判断)

### 7-1. Phase D 実装設計の章は含めない判断 (現状維持で良い)

Phase D 実装設計メモ (`docs/phase_d_implementation_plan.md`、872 行) は本設計書に取り込まれていない。これは:

- Phase D が未着手 (実装着手は帰宅後)
- 「設計書 = 確定事項のみ」「実装設計メモ = generator 向け詳細」の役割分離

→ **取り込まない判断が妥当**。Phase D 完了時に v4.4 として実態を反映する。

### 7-2. response_filter の章は含まれていない

Phase C-0 で追加した `response_filter.py` (内部状態漏出除去) は、24-3 章で「`pico_agent/response_filter.py`」と配置のみ言及されているが、設計概念としての章はない。

→ **必要に応じて第 7-8 章として追加可能** (例: 「ピコ独自の追加要件 → 7-8. response_filter (内部状態漏出除去)」)。ただし優先度は低い (現状で 24-3 章の表中に配置記述あり、テスト 113 件で挙動が固定化されている)。

### 7-3. 帰宅後 P0 タスクの章は含めない (実装計画書側に置く)

P0-1 (vision 黒画像)、P0-2 (UTILITY_* 切替)、P0-3 (_capture_loop 修正) は実装計画書または `docs/capture_loop_fix_design_memo.md` 側に置く方針で OK。設計書には書かない。

---

## 8. 確定版昇格判断 (本セッションでは実行しない)

カイニットの判断材料として:

| 項目 | 状態 |
|---|---|
| 章番号整合性 | ✓ |
| 章間参照リンク | ✓ |
| Phase C-3 / C-4 実装と章 14-4 / 14-5 の整合 | ✓ |
| 二層分離原則と 24 章の整合 | ✓ (16-1 章の 1 行を除く) |
| upstream 同期戦略の明文化 | ✓ |
| 重大欠陥 | なし |
| 微修正候補 | 6 件 (5-1〜5-6、いずれも軽微) |

→ **5-1 (16-1 章 `core/tools/` 行) だけ修正すれば確定版昇格可**。5-2〜5-6 は確定版昇格後の v4.4 で吸収しても問題ない。

---

## 9. 修正実行についての判断

本ファイルは **レビュー結果の記録のみ** で、修正は実行しない。理由:

1. v4.3 素案の確定版化はカイニット帰宅後の判断 (絶対禁止リストに「v4.3 素案の確定版化 (カイニットレビュー前)」と明示)
2. 5-1〜5-6 の修正は素案ファイルへの書き込みになる
3. 確定版昇格と同時に修正を入れる方が、変更履歴 (v4.2 → v4.3 確定) として読みやすい

→ **5-1〜5-6 はカイニットが帰宅後にレビューしてから、確定版昇格時に一括で適用する** のが筋。

---

## 10. 次のタスク

タスク 10 完了。次は:

- **タスク 11**: `outside_period_2_summary.md` 骨子作成 (帰宅時にカイニットが最初に読む入口ドキュメント)
- **タスク 12**: Phase D 着手準備チェック (discord_bridge/ テスト網羅性、`docs/phase_d_dependencies.md`)
- **タスク 13**: pytest 速度改善 (--durations=30 実行、提案メモ)
- **タスク 5**: Day 2 夕の outside_report (18:00 想定)

---

*v4.3 セルフレビュー | 2026-05-20 | タスク 10 完了 | カイニット帰宅後レビュー → 確定版昇格 → 5-1〜5-6 微修正の流れを推奨*
