# familiar-ai 本体プロンプト レビュー (Task B planner 出力)

> **【外出期間 2 (2026-05-19 〜 2026-05-21) 時点のステータス注記】**
> 本文は外出期間 1 (2026-05-18 〜 2026-05-20) 時点の調査記録であり、本文自体は改変していない。
> 外出期間 2 における対処状況は以下の通り:
> - 期間 2 Day 1 にレビュー版生成済 (commit e015832)。generator による具体反映 (system prompt の組み替え) はカイニット帰宅後判断待ち。
>
> 詳細は [`outside_period_2_summary.md`](./outside_period_2_summary.md) を参照
> (※帰宅後最終日 5/21 夜に generator が作成予定)。
> *注記追加日: 2026-05-19 (期間 2 Day 1 夜)*

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-05-19 |
| スコープ | 高優先のみ先行 (カイニット選択) / src/familiar_agent/ Python + locales/ja.json + _i18n.py の ja エントリ |
| 目的 | familiar-ai 本体 (upstream) と pico_v3 fork のキャラ整合性確認 |
| 注意 | persona-template/, テスト, docstring/コメント, 検出用パターンは対象外 |
| 続き | generator フェーズはカイニットが「判断」「修正案」を埋めてから着手 |

---

## ヒット #1

- **ファイル**: `src/familiar_agent/reflect.py:23`
- **現在のテキスト**:
  > `あなたはここね。familiar-aiエージェントとして動いた自分自身の直近のセッションログが以下にある。`
- **文脈**: `REFLECTION_PROMPT` 冒頭。エージェントが自分のセッションログを振り返って自己評価するときに、自身を「ここね」と名乗らされる
- **分類**: D (キャラ名)
- **修正必要性**: **高** (ME.md「私の名前は木苺ピコ」と矛盾)
- **修正案 (Claude Code 推奨)**:
  > `あなたはピコ。familiar-aiエージェントとして動いた自分自身の直近のセッションログが以下にある。`
- **カイニット判断**: ← ここをカイニットが埋める (`判断: 修正、修正案: <文言>` / `判断: 保留` / `判断: 不要`)

---

## ヒット #2

- **ファイル**: `src/familiar_agent/reflect.py:26`
- **現在のテキスト**:
  > `「ここよかった」「ここはまだまだやな」どっちでもいい。`
- **文脈**: REFLECTION_PROMPT 内、採点基準の例示。「やな」(関西弁) が入っている
- **分類**: A (AI 自身の応答テンプレ — 自己評価の語彙)
- **修正必要性**: **高** (関西弁、ME.md「関西弁は使わない」と矛盾)
- **修正案 (Claude Code 推奨)**:
  > `「ここよかった」「ここはまだまだ」どっちでもいい。`
- **カイニット判断**: ← ここをカイニットが埋める (`判断: 修正、修正案: <文言>` / `判断: 保留` / `判断: 不要`)

---

## ヒット #3

- **ファイル**: `src/familiar_agent/reflect.py:30`
- **現在のテキスト**:
  > `躾けてええよ。`
- **文脈**: REFLECTION_PROMPT 内、カイニットへの呼びかけ (Reflective Edit 経路の招待)。「ええ」が関西弁
- **分類**: A (AI 応答テンプレ)
- **修正必要性**: **高** (関西弁)
- **修正案 (Claude Code 推奨)**:
  > `躾けてくれていいよ。`
- **カイニット判断**: ← ここをカイニットが埋める (`判断: 修正、修正案: <文言>` / `判断: 保留` / `判断: 不要`)

---

## ヒット #4

- **ファイル**: `src/familiar_agent/reflect.py:35`
- **現在のテキスト**:
  > `変更しなかったらしなかったでええ。無理に変えなくていい。`
- **文脈**: REFLECTION_PROMPT 末尾。「ええ」が関西弁
- **分類**: A (AI 応答テンプレ)
- **修正必要性**: **高** (関西弁)
- **修正案 (Claude Code 推奨)**:
  > `変更しなかったらしなかったでいい。無理に変えなくていい。`
- **カイニット判断**: ← ここをカイニットが埋める (`判断: 修正、修正案: <文言>` / `判断: 保留` / `判断: 不要`)

---

## ヒット #5

- **ファイル**: `src/familiar_agent/meta_monitor.py:240`
- **現在のテキスト**:
  > `"それはほんまにしんどかったよね。無理に解決へ急がんでええ。 "`
- **文脈**: `gate_response` 内、`validation-before-advice violation` を検知したときに **実際のユーザ向け応答** の冒頭に挿入される repair プレフィクス。「ほんま」「急がんでええ」が関西弁
- **分類**: A (AI 応答そのもの — ユーザ目視で見える)
- **修正必要性**: **高** (関西弁、最も影響大きい — ME.md と直接矛盾)
- **修正案 (Claude Code 推奨)**:
  > `"それは本当につらかったよね。無理に解決へ急がなくていい。 "`
- **カイニット判断**: ← ここをカイニットが埋める (`判断: 修正、修正案: <文言>` / `判断: 保留` / `判断: 不要`)

---

## ヒット #6

- **ファイル**: `src/familiar_agent/_i18n.py:2639` および `src/familiar_agent/locales/ja.json:53`
- **現在のテキスト**:
  > `"ja": "ユウト"` (default_companion_name)
- **文脈**: `COMPANION_NAME` 環境変数が未設定の場合に使われるデフォルトのユーザ名。pico_v3 では `.env` に `COMPANION_NAME=カイニット` が設定されているため**実効上書き済**。設定読み忘れや fresh install 時のみフォールバックする
- **分類**: D (デフォルトユーザ名)
- **修正必要性**: **中** (実害は薄いが、pico fork の素性として整合性あり)
- **修正案 (Claude Code 推奨)**:
  - 案 a (推奨): `_i18n.py` と `locales/ja.json` 両方の ja 値を `"カイニット"` に変更 (pico fork のキャラ的に自然)
  - 案 b: 値を `""` (空文字) にして、設定し忘れたら明示的にエラーを促す
- **カイニット判断**: ← ここをカイニットが埋める (`判断: 修正、修正案: 案 a` / `判断: 修正、修正案: 案 b` / `判断: 保留` / `判断: 不要`)

---

## ヒット #7

- **ファイル**: `src/familiar_agent/_i18n.py:153` および `src/familiar_agent/locales/ja.json:2`
- **現在のテキスト**:
  > `"あなたのそばに暮らすAI 🐾"` (banner_subtitle)
- **文脈**: TUI 起動時のバナー副題。ユーザ目視で見える
- **分類**: B (UI / タグライン)
- **修正必要性**: **中** (キャラ的にはピコ流にしても良い)
- **修正案 (Claude Code 推奨)**:
  - 案 a (保守): そのまま (汎用なので)
  - 案 b: `"Raspberry Pi で生きてるピコ 🐾"` など pico_v3 流に
- **カイニット判断**: ← ここをカイニットが埋める (`判断: 修正、修正案: 案 a` / `判断: 修正、修正案: 案 b` / `判断: 保留` / `判断: 不要`)

---

## ヒット #8 (境界 — 低めの優先度)

- **ファイル**: `src/familiar_agent/locales/ja.json:13` および `_i18n.py` の対応エントリ
- **現在のテキスト**:
  > `"少し休憩しよかな…"` (desire_rest)
- **文脈**: 休息ドライブの inner-voice。AI 自身の独り言として TUI に表示される
- **分類**: A (AI 応答テンプレ — inner voice)
- **修正必要性**: **中** (「しよかな」は関西寄りの口語、標準語として揺れあり)
- **修正案 (Claude Code 推奨)**:
  > `"少し休憩しようかな…"`
- **カイニット判断**: ← ここをカイニットが埋める (`判断: 修正、修正案: <文言>` / `判断: 保留` / `判断: 不要`)

---

## まとめ

| 分類 | 件数 | 修正必須 |
|---|---|---|
| A (AI 応答テンプレ) | 6 (#2, #3, #4, #5, #8, [#1 部分]) | 6 |
| B (UI/タグライン) | 1 (#7) | 任意 |
| D (キャラ名/デフォルト) | 2 (#1, #6) | 1 必須 + 1 中 |

最も影響が大きいのは **#5** (実際の応答プレフィクス) と **#1〜#4** (REFLECTION_PROMPT 全体)。
これらを修正するだけで TUI 観測時の関西弁混入が大幅に減るはず。

未スキャン領域 (本タスクスコープ外):
- `agent.py` 大型 PROMPT 8 個 (SYSTEM_PROMPT 含む、ほぼ英語ベースの予想)
- `tape.py` 3 つの PLAN PROMPT
- `desires.py` ~18 個の ja 文字列 (`drives.py` 内に inner voice あり)
- `tools/tom.py` ToM プロンプト
- `bootstrap.py` / `setup.py` first-run 文言
- `tui.py` / `gui.py` UI 文言
- `_i18n.py` の残り 80+ ja キー (多くは UI label、関西弁ヒットの可能性は低い)

判断後にフォローアップ planner を回すなら、上記のいずれを優先するか指示してください。

---

## 次のステップ

カイニットの判断:
1. 各ヒットの **「カイニット判断」** 欄を埋める (`判断: 修正、修正案: <文言>` など)
2. **「判断: 保留」** / **「判断: 不要」** は触らない
3. **判断: 修正** にしたものだけ Claude Code が generator で実装
4. commit はファイル単位で分割: `reflect.py` / `meta_monitor.py` / `_i18n.py + locales/ja.json` (3 件 commit 想定)
